cache-file: "/var/lib/ntfy/cache.db"
auth-file: "/var/lib/ntfy/auth.db"
auth-default-access: "write-only"
behind-proxy: true
# Subscriber account — generate hash: docker run --rm binwiederhier/ntfy user password --bcrypt <password>
# or python3 -c "import bcrypt, getpass; print(bcrypt.hashpw(getpass.getpass().encode(), bcrypt.gensalt()).decode())"
auth-users:
  - "reader:$2b$12$xwd3xL2VfGwAhn3eUAgdnubU0P9hsPi9mrI6ibo5EbsjUg0RR3Zki:user"
auth-access:
  - "reader:*:ro"
